# Auto-synced file, managed by [dealroom/mothership](https://github.com/dealroom/mothership)
# The changes to this file will be automatically overwritten on the next sync. Do not edit by hand!

name: Reusable Trivy Scan

on:
  workflow_call:
    inputs:
      severity:
        description: Severities to include (CRITICAL,HIGH,MEDIUM,LOW,UNKNOWN)
        required: false
        type: string
        default: CRITICAL,HIGH
      exit-code:
        description: Exit code when vulnerabilities are found
        required: false
        type: string
        default: "1"
    outputs:
      sbom-path:
        description: Path to the generated CycloneDX SBOM file
        value: ${{ jobs.trivy-scan.outputs.sbom-path }}

permissions: {}

env:
  # renovate: datasource=github-releases depName=aquasecurity/trivy
  TRIVY_VERSION: v0.68.2

jobs:
  trivy-scan:
    name: Trivy Security Scan
    runs-on: ubuntu-slim
    permissions:
      contents: read
      pull-requests: write
    outputs:
      sbom-path: ${{ steps.sbom.outputs.sbom-path }}
    steps:
      - name: Checkout code
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          persist-credentials: false
      - name: Setup linter (generates .trivyignore)
        uses: ./.github/actions/setup-linter
        with:
          configure-ignores: true
      - name: Run Trivy vulnerability scanner in repo mode
        id: trivy
        uses: aquasecurity/trivy-action@b6643a29fecd7f34b3597bc6acb0a98b03d33ff8 # 0.33.1
        with:
          scan-type: fs
          scan-ref: .
          format: table
          output: trivy-results.txt
          severity: ${{ inputs.severity }}
          exit-code: ${{ inputs.exit-code }}
          ignore-unfixed: true
          trivyignores: .trivyignore
          hide-progress: false
          version: ${{ env.TRIVY_VERSION }}
        env:
          TRIVY_DISABLE_VEX_NOTICE: "true"
        continue-on-error: true
      - name: Display Trivy results
        if: always()
        run: |
          if [ -f trivy-results.txt ]; then
            echo "=== Trivy Scan Results ==="
            cat trivy-results.txt
          else
            echo "No Trivy results file found"
          fi
      - name: Check Trivy results and fail
        if: always() && steps.trivy.outcome == 'failure' && inputs.exit-code == '1'
        run: |
          echo "Trivy scan found vulnerabilities. Failing workflow as requested."
          exit 1
      - name: Generate SBOM with Trivy
        id: sbom
        if: always()
        run: |
          trivy fs . --format cyclonedx --output sbom.json --severity "${INPUTS_SEVERITY}"
          echo "sbom-path=sbom.json" >> "$GITHUB_OUTPUT"
        env:
          TRIVY_DISABLE_VEX_NOTICE: "true"
          INPUTS_SEVERITY: ${{ inputs.severity }}
      - name: Generate timestamp
        id: timestamp
        if: always()
        run: echo "timestamp=$(date +%s)" >> "$GITHUB_OUTPUT"
      - name: Generate comment body
        id: comment
        if: always()
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8.0.0
        env:
          TRIVY_OUTCOME: ${{ steps.trivy.outcome }}
        with:
          script: |
            const fs = require('fs');
            const runUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            let comment = '';
            const outcome = process.env.TRIVY_OUTCOME;
            if (outcome === 'success') {
              comment += '✅ **No vulnerabilities found!**\n\n';
              comment += `**SBOM:** [Download from workflow artifacts](${runUrl}#artifacts)`;
            } else {
              comment += '⚠️ **Vulnerabilities detected**\n\n';
              if (fs.existsSync('trivy-results.txt')) {
                const report = fs.readFileSync('trivy-results.txt', 'utf8');
                const lines = report.split('\n');
                // Extract summary if available
                const totalLine = lines.find(line => line.includes('Total:'));
                if (totalLine) {
                  comment += `**Summary:** ${totalLine.trim()}\n\n`;
                }
                // Add detailed report
                comment += '<details>\n<summary>Click to see detailed report</summary>\n\n';
                comment += '```\n';
                if (report.length > 5000) {
                  comment += report.substring(0, 5000);
                  comment += '\n```\n</details>\n\n';
                  comment += `_Report truncated. [View full report in workflow artifacts](${runUrl}#artifacts)_\n\n`;
                } else {
                  comment += report;
                  comment += '\n```\n</details>\n\n';
                }
              } else {
                comment += `_[View detailed report in workflow artifacts](${runUrl}#artifacts)_\n\n`;
              }
              comment += `**SBOM:** [Download from workflow artifacts](${runUrl}#artifacts)`;
            }
            // Ensure we always output something
            if (!comment) {
              comment = '⚠️ **Error generating scan report**\n\nPlease check the workflow logs for details.';
            }
            core.setOutput('comment_body', comment);
      - name: Comment PR with scan results
        if: always()
        uses: ./.github/actions/github-pr-comment
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          comment-identifier: "trivy-scan-results"
          comment-tag: "security"
          comment-title: "🔒 Dependency Scan"
          mode: "grouped"
          comment-body: ${{ steps.comment.outputs.comment_body }}
      - name: Upload SBOM artifact
        if: success() || failure()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: trivy-sbom-${{ github.run_id }}-${{ steps.timestamp.outputs.timestamp }}
          path: sbom.json
          retention-days: 30
      - name: Upload Trivy results artifact
        if: success() || failure()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: trivy-results-${{ github.run_id }}-${{ steps.timestamp.outputs.timestamp }}
          path: trivy-results.txt
          retention-days: 30
